/** Mock bus pricing and cancellation policy live in @zproo/catalog (shared with the static website). */
export {
  BUS_AC_GST_PERCENT,
  BUS_CANCELLATION_POLICY,
  busSeatFare,
  type SeatFare,
} from '@zproo/catalog';
